meta {
  name: SearchDocName
  type: http
  seq: 8
}

post {
  url: {{url}}/lightweightcmis/browser/default/
  body: multipartForm
  auth: inherit
}

body:multipart-form {
  cmisaction: query
  statement: select * from cmis:document where cmis:name like 'File%'
}
