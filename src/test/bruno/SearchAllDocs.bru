meta {
  name: SearchAllDocs
  type: http
  seq: 7
}

post {
  url: {{url}}/lightweightcmis/browser/default/
  body: multipartForm
  auth: inherit
}

body:multipart-form {
  cmisaction: query
  statement: select * from cmis:document
}
