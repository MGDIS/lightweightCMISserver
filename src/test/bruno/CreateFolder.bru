meta {
  name: CreateFolder
  type: http
  seq: 3
}

post {
  url: {{url}}/lightweightcmis/browser/default/root/
  body: multipartForm
  auth: inherit
}

body:multipart-form {
  cmisaction: createFolder
  propertyId[0]: cmis:objectTypeId
  propertyValue[0]: cmis:folder
  propertyId[1]: cmis:name
  propertyValue[1]: MyBusiness
  file: @file(bruno.json)
}
