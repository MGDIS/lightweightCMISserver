meta {
  name: GetContentById
  type: http
  seq: 12
}

get {
  url: {{url}}/lightweightcmis/browser/default/root/?cmisselector=content&objectId={{docId}}
  body: multipartForm
  auth: inherit
}

params:query {
  cmisselector: content
  objectId: {{docId}}
  ~returnVersion: latest
  ~succinct: true
}
